# TODO đang thực hiện — nestjs — StartFlow Agent

> Được duy trì bởi `_refs/orchestration/tail/auto-task-tracker.md`.
> Định dạng: `[ ]` đang mở / `[x] (YYYY-MM-DD)` hoàn tất / `[!]` bị chặn.

## Hiện tại

- [ ] Deploy thay đổi auth lên `dev` và kiểm tra token người dùng thật từ `portal-ops`.

## Tiếp theo

## Sau này

## Đang bị chặn

- [!] Kiểm tra live cần `KEYCLOAK_SECRET` trong GitHub `STARTFLOW_DEV` và audience `INTEGRATION_API` trong access token người dùng.

## Hoàn tất (7 ngày gần nhất)

## Cũ
